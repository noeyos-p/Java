int i = 0;
for (int j = 0; j < 10; j++)
이라고 할때
상단에 있는 i 값은 밑에서 사용가능하지만
하단에 있는 j의 값은 위에서 사용 불가능함
비교 판단을 해서 누적되는 값이 150 이상이 되면?? // 이게 뭔말이여

break;
// 해당 루프를 빠져나올 때 사용
// 중복 루프면 break; 위 단계로 다시 감..
// 쓸데없는 for loop 동작을 멈추게 함
// 완전하게 밖으로 빠져나감

continue;
// continue 아랫부분 실행 안하고 다시 반복문 실행
// 밑에 있는 애들을 실행하지 않고 다시 올라
// 자기 밑에 남아있는 애들을 실행하지 않고 다시 올라감

return
// 프로그램이 완전 종료됨
// 조건을 만나면 실행하고 있던 프로그램이 종료됨

문자열 같다(==)는 .equals를 써야함

switch 구문
// case -> or : 둘다 상관없음
// case 하나마다 break;를 적어줘야 그 밑으로 내려가지 않음
// case 문은 범위로 범주로 포함된다는 기능 없음

범위는 오직 if만 가능

for, while, do ~ while
for(int i = 1, i <= 5; i++) {
  반복실행내용
  break; / continue; / return;
}

while (조건) {
  실행문;
}

do {
  실행문;
} while (조건) {
  실행문;
}

while (true) {
  실행문;
}
-> 무한루프

정해둔 값을 다시 쓰려면 초기화 해야함